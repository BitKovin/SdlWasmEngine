<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- This project has zero external package dependencies (BCL only),
       so restore doesn't need to reach any feed. Keeping the source
       list empty means `dotnet build`/`publish` work fully offline. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
